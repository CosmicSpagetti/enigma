class Enigma

  # def encrypt(message, key = 'random', date = Time.now.strftime("%d%m%y"))
    # { key.
    #   encryption: encrypted_message(offset(date, method(key)))),
      ## takes offset separates all the letters and shifts them individually
      ## maybe ord for shifts, cycle through shifts

      # key: method(key),
      ## key object? random key needs to know to pad
      ## need to divide key to A,B,C,D keys
      # date: date
      ## needs to take key set and create offset for the encryption
    # }

    ## need to start with key for offset and then apply to encrypt
  # end


end
